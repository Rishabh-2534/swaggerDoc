post:
  operationId: getFilesFromSiteB2B
  tags:
    - Files
  summary: Get files
  description: |
    This API lets you search for files using multiple parameters including siteId, contentId, file id, and various filtering options.
  requestBody:
    description: Get Files using site ID, content ID, or file ID
    content:
      application/json:
        schema:
          $ref: '../../../schemas/content/b2b/request/get_files.yaml'
    required: true
  responses:
    200:
      $ref: "../../../responses/content/getFiles/get_files_success.yaml"
    400:
      $ref: "../../../responses/content/getFiles/get_files_error.yaml"
    401:
      $ref: "../../../responses/common/unauthorized.yaml"
    403:
      $ref: "../../../responses/common/forbidden.yaml" 