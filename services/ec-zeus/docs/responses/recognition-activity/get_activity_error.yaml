description: "Internal Server Error"
content:
  application/json:
    schema:
      type: "object"
      properties:
        status:
          type: "string"
          enum:
            - "error"
        message:
          type: "string"
          example: "Unknown error occurred while fetching recognition activity, contact support"
      required:
        - status
        - message