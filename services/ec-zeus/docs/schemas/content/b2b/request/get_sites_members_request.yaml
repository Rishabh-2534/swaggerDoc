- name: size
  in: query
  description: Maximum number of results to return
  schema:
    type: integer
    minimum: 1
    maximum: 1000
    default: 50
  required: false

- name: nextPageToken
  in: query
  description: Token for fetching the next page of results
  schema:
    type: integer
  required: false

- name: userId
  in: query
  description: Optional user ID to filter the results
  schema:
    type: string
    format: uuid
  required: false

- name: siteId
  in: query
  description: Optional site ID to filter the results
  schema:
    type: string
    format: uuid
  required: false

- name: modifiedAfter
  in: query
  description: Only return results modified after this date-time
  schema:
    type: string
    format: date-time
  required: false

- name: roleType
  in: query
  description: Optional role type to filter the results
  schema:
    type: string
    enum:
      - owner
      - manager
      - content_manager
      - member
  required: false

- name: includeDeleted
  in: query
  description: Whether to include deleted members in the response
  schema:
    type: boolean
    default: false
  required: false
