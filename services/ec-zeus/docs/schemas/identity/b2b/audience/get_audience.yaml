allOf:
  - $ref: './get_audiences_item.yaml'
  - type: object
    properties:
      sourceType:
        type: string
        description: Source type of the audience
        example: "app_managed"
        enum:
          - app_managed
        nullable: true
      rule:
        $ref: './audience_rule_schema.yaml'
        description: Audience rule configuration with flexible AND/OR structure
        nullable: true
