content:
  application/json:
    schema:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 1000
          description: Comment text to be added to the ticket
          example: This is a comment on the ticket.
      additionalProperties: false